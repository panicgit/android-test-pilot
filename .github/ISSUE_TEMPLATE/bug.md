---
name: Bug report
about: Report a problem with android-test-pilot
labels: bug
---

## Environment
- OS: <e.g. macOS 14.4, Ubuntu 22.04>
- Node.js: <output of `node -v`>
- android-test-pilot: <output of `npm ls @panicgit/android-test-pilot`>
- ADB: <output of `adb --version`>
- Device: <Pixel 8 / API 34 / emulator or physical>

## Description
<What happened? What did you expect to happen?>

## Reproduction

<Minimal repro — ideally the scenario file, the `atp_run_step` invocation,
and the MCP response.>

```json
// atp_run_step response
```

## Logs
<Run the MCP server with `DEBUG=1` if applicable and paste the relevant
`trace()` / `error()` lines below.>

```
```

## app-map artifacts
<If relevant, paste the contents of `.claude/app-map/*.json` (redact
anything sensitive).>
